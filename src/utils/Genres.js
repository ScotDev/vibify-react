// Takes an array of genres and returns an array of genres objects.
// {
//   "spotifyName": "edm",
//   "vibifyName": "EDM",
// }
// The UI will use the vibifyName to display the genre. It already capitalizes the first letter of each word,
// but some like edm need to be in all caps, and others need to be formatted to removed dashes and "and"

// const parseGenres = async (genres) => {
// const genreObjects = [];

//     for (let i = 0; i < genres.length; i++) {

// };
